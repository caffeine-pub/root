[package]
name = "pointsto"
version = "0.1.0"

[package.scripts]
start = "tsx src/index.ts"

[package.dependencies]
ts-hash = "workspace:*"
ts-arena = "workspace:*"

[package.devDependencies]
typescript = "5.9.3"
vitest = "^4.1.0"

[tsconfig.compilerOptions]
outDir = "dist"
rootDir = "src"
